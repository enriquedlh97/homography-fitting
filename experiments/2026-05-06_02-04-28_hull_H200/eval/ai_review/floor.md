# floor

Floor band Red Bull replacement on the blue stripe over the MELBOURNE wordmark. Color and geometry are excellent; coverage holds across all motion strips with no leak in static (non-walkover) frames. A faint halo at the wordmark and a small reflex around the bull rings remain — equivalent to the P3-A29/a3 baseline since obj_4 (the changed object) is not the floor logo. No regressions from the feather change.
