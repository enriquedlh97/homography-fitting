# walkover — synthesis (BTN + mask_dilate=8 + alpha_feather=8)

The walkover window 685-723 is the strictest visual test for the synthesis hypothesis: this is where motion would expose any frame-to-frame seam introduced by feather=8. Across all five forensic sheets (entry f0685, pre-contact f0694, contact f0704, post-contact f0713, exit f0723) the composite panel shows a clean Red Bull patch under the player's feet with no perimeter rectangle, no halo at the wordmark, no letter-edge ghost. Consecutive-frames strip 685-724 confirms the patch is locked frame-to-frame — there is no walking boundary, no shimmer, no reveal at edges. Original-survival diagnostic still flags residual MELBOURNE bleed (the underlying ad is partially showing through during high-occlusion frames), but that is a separate axis from the seam test. Player contact shadow remains the lone weakness — feet do not darken the mark — which the synthesis was not designed to address.
