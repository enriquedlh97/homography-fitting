# back — feather=4 variant (a1)

Back banner Red Bull lockups are stable across the run. Letter edges and the rampant-bull silhouettes stay clean against the purple venue scrim with no perimeter halo and no letter-edge reflex visible at strip resolution. Color and brightness match the original Kia scrim convincingly. Geometry rides the back curtain plane without warping. The obj_4 inpaint_feather_px=4 change targets left+floor regions, so the back is essentially unchanged from baseline — scoring identically.
