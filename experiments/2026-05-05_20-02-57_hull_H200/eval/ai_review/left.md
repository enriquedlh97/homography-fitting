# left

A scrubbing viewer would see the Red Bull wordmark sitting cleanly on the bright blue side-banner panel. The patch boundary is invisible — no rectangular seam against the panel — and the perimeter glow that earlier P3-A4/a2 runs introduced is gone. There is still a faint softness at the top edges of the letters and around the bulls' wing-tips, but it is mild enough to read as paint rather than as a digital artifact. Hue and brightness match the panel tone across the duration. Quality matches the P3-A5 baseline; this region is unaffected by the floor `erase_text` flag.
