# walkover — visual review

The walkover window (685–723) confirms the floor-region tradeoff. halo_presence scores a clean 5 — across all five forensic sheets (entry, pre-contact, contact, post-contact, exit) there is no luminance halo around the Red Bull wordmark or bulls. Tight feather=2 successfully kills the canonical Melbourne floor halo case.

The penalty is edge_seam_visibility (3). The consecutive_frames strip shows a thin rectangular perimeter line at the top edge of the Red Bull patch, persistent across the window. BTN's stable per-frame H prevents the seam from walking (no pop on contact frames), but the line is locatable on slow scrub. Player feet occlude the patch correctly; geometry holds.

Original-survival panel still shows residual MELBOURNE bleed-through (single-pass synthesis limit, prior-known). Player contact shadow unchanged from earlier runs — the mark stays opaque under feet rather than darkening. Net verdict: equal halo result to a great run, but the seam is the cost of going to feather=2.
