# walkover — viewer notes

The walkover window (685-723) is where this run breaks. On the consecutive_frames strip the Red Bull mark holds rigid as the player crosses, which is fine on its own, but the forensic sheets show two real problems. First, the 'original survival' map is hot red across the Melbourne wordmark area on every keyframe — the original 'ELBOUR' text is bleeding through under the composite, meaning the inpaint clean plate is not fully erasing the underlying ad. The 'suspected leak overlay' visualizes this directly. Second, the brighter rectangular patch from the floor region persists here, so the ad still reads as a sticker on the court rather than paint. Player feet are drawn over the logo (occlusion IoU is high) but there is no contact shadow under the feet on the painted ad.
