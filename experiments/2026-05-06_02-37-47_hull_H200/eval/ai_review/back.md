# back — inpaint_method=ns variant (a2)

Back curtain Red Bull lockups stable across the run. No halo, no letter-edge reflex visible at strip resolution. Color and brightness match the original Kia scrim. The inpaint_method=ns change targets left+floor regions, so back is essentially unchanged from baseline.
