# full — inpaint_method=ns variant (a2)

Full-frame composite is convincing at broadcast zoom. Surface insertions don't jump out as composites. The inpaint method change is invisible at full-frame zoom — only resolvable in per-region close-ups.
