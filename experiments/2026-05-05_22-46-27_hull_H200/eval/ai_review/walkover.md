# walkover

Frames 685-723: a player walks across the floor band Red Bull insertion. A scrubbing viewer would read the walkover as believable. Legs and feet occlude the bulls and wordmark correctly across consecutive frames — the obj_4 mask_dilate=4 hull holds shape across the traversal without nibbling player anatomy. With shadow_strength=0.6 the contact shadow at f0704 and f0713 has the weight to plant feet on the court, including post-contact motion blur frames where earlier candidates went weightless. No jitter, no breathing, color/geometry stable.

The remaining issue the forensic sheets surface: the "suspected leak overlay" panels at f0685 entry and especially f0723 exit show residual MELBOURNE wordmark pixels bleeding through behind the Red Bull lockup. The leak is faint in the direct composite column — a viewer probably would not pause on it — but it is the cleanest description of where this run still falls short of the v68 quality bar on the floor surface.
