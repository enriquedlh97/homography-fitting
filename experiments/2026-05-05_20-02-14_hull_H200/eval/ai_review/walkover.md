# walkover — sub-agent prose

A scrubbing viewer watching frames 685-723 sees the player walk across the Red Bull floor lockup with stable occlusion and no jitter. The consecutive_frames panel does not tell on the substitution: legs and feet pass over the lockup naturally, contact shadows ground the figure, and the floor band stays uniform. At pre-contact, contact, and post-contact (motion-blur) frames the lockup is unscathed by player crossing.

The forensic 'suspected leak overlay' panels do reveal residual MELBOURNE letterforms bleeding through the composite — most clearly at the f0723 exit and the f0685 entry. The leak is not visible in the direct composite scrub at this resolution but exists if a delta diagnostic is run. Color, geometry and contact-shadow grounding are all in good shape. Net behaviour is comparable to P3-A29 — leak residue at the same edges, occlusion equally clean.
